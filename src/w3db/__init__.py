"""
W3DB — in-process database layer for the W3 execution model.

Domains: XIZ (execution trace) → TUF (state snapshot) → FBD (failed boundary)
         → WHB (law/patch) → PRX (perception output)
"""
