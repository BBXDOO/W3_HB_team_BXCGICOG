# BOX Templates

Templates are immutable reference sources in normal use. Every template must
contain YAML front matter with an ID, version, scope, boundary, deny rules,
owner, status, and creation date. Copy to a reviewed workspace before editing.
