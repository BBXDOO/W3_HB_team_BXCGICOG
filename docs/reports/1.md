# สรุปการแก้ไข test-harness.md

## การเปลี่ยนแปลงทั้งหมด

ไฟล์นี้สรุปบรรทัดที่แก้ไขและส่วนที่เสนอให้แก้ใน `ChatGPT/testcases/test-harness.md`

---

## 1. แก้ไข Expected Banner ในตัวอย่าง T0 (บรรทัด 25)

**เดิม:**
```markdown
- run main → expect banner `W3 Hybrid Engine online`
```

**แก้เป็น:**
```markdown
- run main → expect banner `W3 Hybrid Engine: ONLINE`
```

**เหตุผล:** ปรับให้ตรงกับ output จริงของ engine

---

## 2. ปิด JSON Code Block และแก้ไข Glossary (บรรทัด 67-79)

**เดิม:**
```markdown
  "constraints": ["no network", "local only", "..."]
}

Glossary:

target = จุดที่อยากทดสอบ (เช่น engine_boot, logger, config_loader)

goal = พฤติกรรมที่ต้องพิสูจน์

risk = ช่วย Gemini เลือกความเข้มข้นการตรวจ

constraints = เงื่อนไขสภาพแวดล้อม
```

**แก้เป็น:**
```markdown
  "constraints": ["no network", "local only", "..."]
}
```

**Glossary:**

- `target` = จุดที่อยากทดสอบ (เช่น engine_boot, logger, config_loader)
- `goal` = พฤติกรรมที่ต้องพิสูจน์
- `risk` = ช่วย Gemini เลือกความเข้มข้นการตรวจ
- `constraints` = เงื่อนไขสภาพแวดล้อม
```

**เหตุผล:** 
- เพิ่ม ``` เพื่อปิด JSON code block
- จัดรูปแบบ Glossary เป็น bulleted list
- เพิ่ม ** ** ให้ Glossary heading

---

## 3. เพิ่ม Section Header และ YAML Code Block สำหรับ Section 3 (บรรทัด 81-111)

**เดิม:**
```markdown
3. Standard Test Output (สิ่งที่ ChatGPT ต้องสร้าง)

รูปแบบ output หนึ่ง test case (human-readable + machine-mappable):

id: T1-logger-basic-001
level: T1
target: logger
title: "Logger writes heartbeat event"

precondition:
  - "engine booted successfully"

steps:
  - "trigger system_heartbeat once"
  - "wait 100ms"

expected:
  - "runtime log file exists"
  - "contains event_type=system_heartbeat"
  - "contains module=Engine"

failure_path:
  - "no file"
  - "file exists but no matching event"

log:
  risk: L2
  owner: ChatGPT
  route_next: Gemini

ข้อบังคับ:

ทุกเคส ต้องมี failure_path

ทุกเคส ต้องระบุ route_next (ใครตรวจต่อ)
```

**แก้เป็น:**
```markdown
## 3. Standard Test Output (สิ่งที่ ChatGPT ต้องสร้าง)

รูปแบบ output หนึ่ง test case (human-readable + machine-mappable):

```yaml
id: T1-logger-basic-001
level: T1
target: logger
title: "Logger writes heartbeat event"

precondition:
  - "engine booted successfully"

steps:
  - "trigger system_heartbeat once"
  - "wait 100ms"

expected:
  - "runtime log file exists"
  - "contains event_type=system_heartbeat"
  - "contains module=Engine"

failure_path:
  - "no file"
  - "file exists but no matching event"

log:
  risk: L2
  owner: ChatGPT
  route_next: Gemini
```

**ข้อบังคับ:**

- ทุกเคส ต้องมี `failure_path`
- ทุกเคส ต้องระบุ `route_next` (ใครตรวจต่อ)
```

**เหตุผล:**
- เพิ่ม ## prefix สำหรับ section header
- เพิ่ม ```yaml code block รอบ test case example
- จัดรูปแบบ "ข้อบังคับ" เป็น bulleted list พร้อม bold

---

## 4. เพิ่ม Section Header และลบบรรทัดว่างเกิน ใน Section 4 (บรรทัด 120-132)

**เดิม:**
```markdown
4. Scenario Design Workflow

ChatGPT ใช้ flow นี้ตอนออกแบบ test:

1. รับ input spec (JSON ด้านบน)


2. ระบุ level + target ให้ชัด


3. แตกเป็น precondition / steps / expected / failure_path


4. ผูกกับ log schema / module name จริงใน W3


5. ใส่ route_next (ส่วนใหญ่ → Gemini / Copilot-Gm)


6. ตรวจ self-consistency (steps สัมพันธ์กับ expected)


7. ส่งออกเป็น YAML/JSON พร้อมใช้งาน (แต่ในไฟล์นี้ใช้เป็น text spec เพื่ออ่านง่าย)
```

**แก้เป็น:**
```markdown
## 4. Scenario Design Workflow

ChatGPT ใช้ flow นี้ตอนออกแบบ test:

1. รับ input spec (JSON ด้านบน)
2. ระบุ level + target ให้ชัด
3. แตกเป็น precondition / steps / expected / failure_path
4. ผูกกับ log schema / module name จริงใน W3
5. ใส่ route_next (ส่วนใหญ่ → Gemini / Copilot-Gm)
6. ตรวจ self-consistency (steps สัมพันธ์กับ expected)
7. ส่งออกเป็น YAML/JSON พร้อมใช้งาน (แต่ในไฟล์นี้ใช้เป็น text spec เพื่ออ่านง่าย)
```

**เหตุผล:**
- เพิ่ม ## prefix สำหรับ section header
- ลบบรรทัดว่างเกินระหว่างแต่ละข้อ

---

## 5. เพิ่ม Section Header และ YAML Code Block ใน Section 5.1 (บรรทัด 139-163)

**เดิม:**
```markdown
5. Example Scenarios

5.1 T0 — Engine Online

id: T0-engine-online-001
level: T0
target: engine_boot
title: "Engine prints online banner"

steps:
  - "run: python src/main.py"

expected:
  - "stdout contains 'W3 Hybrid Engine online'"
  - "process remains alive at least 3s"

failure_path:
  - "no output"
  - "process exits with non-zero code"

log:
  risk: L1
  route_next: Copilot-Gm

```

**แก้เป็น:**
```markdown
## 5. Example Scenarios

### 5.1 T0 — Engine Online

```yaml
id: T0-engine-online-001
level: T0
target: engine_boot
title: "Engine prints online banner"

steps:
  - "run: python src/main.py"

expected:
  - "stdout contains 'W3 Hybrid Engine: ONLINE'"
  - "process remains alive at least 3s"

failure_path:
  - "no output"
  - "process exits with non-zero code"

log:
  risk: L1
  route_next: Copilot-Gm
```
```

**เหตุผล:**
- เพิ่ม ## prefix สำหรับ main section header
- เพิ่ม ### prefix สำหรับ subsection header
- เพิ่ม ```yaml code block รอบ test case
- แก้ไข expected output เป็น 'W3 Hybrid Engine: ONLINE'
- ลบบรรทัดว่างท้าย

---

## 6. เพิ่ม Subsection Header และ YAML Code Block ใน Section 5.2 (บรรทัด 167-193)

**เดิม:**
```markdown
5.2 T1 — Config Loader Error Handling

id: T1-config-invalid-001
level: T1
target: config_loader
title: "Invalid JSON config is handled gracefully"

precondition:
  - "prepare malformed config file at config/settings.json"

steps:
  - "run: python src/main.py"

expected:
  - "engine does not crash"
  - "stderr contains 'config error'"
  - "log contains event_type=config_error"

failure_path:
  - "uncaught exception"
  - "no error log"

log:
  risk: L3
  route_next: Gemini

```

**แก้เป็น:**
```markdown
### 5.2 T1 — Config Loader Error Handling

```yaml
id: T1-config-invalid-001
level: T1
target: config_loader
title: "Invalid JSON config is handled gracefully"

precondition:
  - "prepare malformed config file at config/settings.json"

steps:
  - "run: python src/main.py"

expected:
  - "engine does not crash"
  - "stderr contains 'config error'"
  - "log contains event_type=config_error"

failure_path:
  - "uncaught exception"
  - "no error log"

log:
  risk: L3
  route_next: Gemini
```
```

**เหตุผล:**
- เพิ่ม ### prefix สำหรับ subsection header
- เพิ่ม ```yaml code block รอบ test case
- ลบบรรทัดว่างท้าย

---

## 7. เพิ่ม Subsection Header และ YAML Code Block ใน Section 5.3 (บรรทัด 197-223)

**เดิม:**
```markdown
5.3 T2 — Module Load Log

id: T2-modules-load-001
level: T2
target: module_loader
title: "All declared modules are logged as loaded"

precondition:
  - "config declares modules: [Gemini, Copilot-Gm, Grok, DeepSeek, ChatGPT]"

steps:
  - "run: python src/main.py"
  - "wait 500ms"

expected:
  - "log has one module_load event per module"
  - "no unknown module appears"

failure_path:
  - "missing module_load for any declared module"
  - "extra module not in config"

log:
  risk: L2
  route_next: Gemini

```

**แก้เป็น:**
```markdown
### 5.3 T2 — Module Load Log

```yaml
id: T2-modules-load-001
level: T2
target: module_loader
title: "All declared modules are logged as loaded"

precondition:
  - "config declares modules: [Gemini, Copilot-Gm, Grok, DeepSeek, ChatGPT]"

steps:
  - "run: python src/main.py"
  - "wait 500ms"

expected:
  - "log has one module_load event per module"
  - "no unknown module appears"

failure_path:
  - "missing module_load for any declared module"
  - "extra module not in config"

log:
  risk: L2
  route_next: Gemini
```
```

**เหตุผล:**
- เพิ่ม ### prefix สำหรับ subsection header
- เพิ่ม ```yaml code block รอบ test case
- ลบบรรทัดว่างเกิน

---

## 8. เพิ่ม Section Header และจัดรูปแบบ Section 6 (บรรทัด 228-254)

**เดิม:**
```markdown
6. Integration with W3

ตำแหน่งไฟล์ test spec: ChatGPT/testcases/

Copilot-Gm

อ่าน test spec → สร้าง test script จริง (pytest / custom runner)

รันใน CI / local

แจ้งผลกลับ engine + BBX19


Gemini

ตรวจความครบถ้วน / risk coverage

เช็คว่า test ครอบ use-case สำคัญของ engine

review ว่า expected behavior align กับ system spec


Grok

จับ pattern ของ bug ที่เคยเจอจาก failure_path

สร้าง knowledge base ของ failure signature

ช่วยชี้ pattern anti-design


DeepSeek

ใช้ test spec วางแผน scaling / optimization

วัด performance path ที่ critical

เสนอ refactor ที่ไม่ทำลาย coverage เดิม
```

**แก้เป็น:**
```markdown
## 6. Integration with W3

**ตำแหน่งไฟล์ test spec:** `ChatGPT/testcases/`

### Copilot-Gm

- อ่าน test spec → สร้าง test script จริง (pytest / custom runner)
- รันใน CI / local
- แจ้งผลกลับ engine + BBX19

### Gemini

- ตรวจความครบถ้วน / risk coverage
- เช็คว่า test ครอบ use-case สำคัญของ engine
- review ว่า expected behavior align กับ system spec

### Grok

- จับ pattern ของ bug ที่เคยเจอจาก failure_path
- สร้าง knowledge base ของ failure signature
- ช่วยชี้ pattern anti-design

### DeepSeek

- ใช้ test spec วางแผน scaling / optimization
- วัด performance path ที่ critical
- เสนอ refactor ที่ไม่ทำลาย coverage เดิม
```

**เหตุผล:**
- เพิ่ม ## prefix สำหรับ main section header
- เพิ่ม ### prefix สำหรับแต่ละ module subsection
- จัดรูปแบบแต่ละ module เป็น bulleted list
- เพิ่ม bold และ code formatting ให้ path header

---

## 9. เพิ่ม Section Header และจัดรูปแบบ Section 7 (บรรทัด 260-284)

**เดิม:**
```markdown
7. Anti-patterns (ถือว่า FAIL)

กรณีต่อไปนี้ เทสถือว่า "ใช้ไม่ได้":

เคสที่เขียนแค่ "ผ่านได้" โดยไม่ทำอะไร

expected กว้างแบบตีความได้หลายแบบ

ไม่มี failure_path

ไม่มีการผูกกับ log หรือ behavior จริงในระบบ

narrative ยาว แต่ไม่ actionable

route_next ไม่ชัด (ไม่รู้ใครดูต่อ)


> ถ้า test case ไม่:

actionable

reproducible

traceable ไปยัง module จริง
⇒ ให้ mark ว่า FAIL
```

**แก้เป็น:**
```markdown
## 7. Anti-patterns (ถือว่า FAIL)

กรณีต่อไปนี้ เทสถือว่า "ใช้ไม่ได้":

- เคสที่เขียนแค่ "ผ่านได้" โดยไม่ทำอะไร
- expected กว้างแบบตีความได้หลายแบบ
- ไม่มี failure_path
- ไม่มีการผูกกับ log หรือ behavior จริงในระบบ
- narrative ยาว แต่ไม่ actionable
- route_next ไม่ชัด (ไม่รู้ใครดูต่อ)

> ถ้า test case ไม่:
> - actionable
> - reproducible
> - traceable ไปยัง module จริง
>
> ⇒ ให้ mark ว่า FAIL
```

**เหตุผล:**
- เพิ่ม ## prefix สำหรับ section header
- จัดรูปแบบรายการเป็น bulleted list
- แก้ไข blockquote formatting ให้ถูกต้อง

---

## 10. เพิ่ม Section Header และจัดรูปแบบ Section 8 (บรรทัด 290-314)

**เดิม:**
```markdown
8. Definition of Done (DOD)

ชุด testcases จาก ChatGPT ถือว่า "พร้อมส่งให้เอเจนท์" เมื่อ:

1. ครอบคลุม T0–T2 อย่างน้อยอย่างละ 1 เคสสำคัญ


2. ทุกเคสมี id, level, target, steps, expected, failure_path ครบ


3. มี route_next ชัดเจนสำหรับแต่ละเคส


4. สามารถแปลงเป็น test script ได้โดยไม่ต้องเดาเพิ่ม (human หรือ agent เขียน runner ได้ตรงๆ)



เกณฑ์สุดท้าย:

> If human can run it,
and engine can log it,
and Gemini can judge it,
⇒ Test Harness = PASS
```

**แก้เป็น:**
```markdown
## 8. Definition of Done (DOD)

ชุด testcases จาก ChatGPT ถือว่า "พร้อมส่งให้เอเจนท์" เมื่อ:

1. ครอบคลุม T0–T2 อย่างน้อยอย่างละ 1 เคสสำคัญ
2. ทุกเคสมี id, level, target, steps, expected, failure_path ครบ
3. มี route_next ชัดเจนสำหรับแต่ละเคส
4. สามารถแปลงเป็น test script ได้โดยไม่ต้องเดาเพิ่ม (human หรือ agent เขียน runner ได้ตรงๆ)

**เกณฑ์สุดท้าย:**

> If human can run it,
> and engine can log it,
> and Gemini can judge it,
> ⇒ Test Harness = PASS
```

**เหตุผล:**
- เพิ่ม ## prefix สำหรับ section header
- ลบบรรทัดว่างเกินระหว่างแต่ละข้อ
- เพิ่ม bold ให้ "เกณฑ์สุดท้าย"
- แก้ไข blockquote formatting ให้ถูกต้อง

---

## สรุป

### สถิติการเปลี่ยนแปลง
- **บรรทัดที่เพิ่ม:** 62 บรรทัด
- **บรรทัดที่ลบ:** 112 บรรทัด
- **บรรทัดสุทธิที่ลด:** 50 บรรทัด (จาก 337 เหลือ 286 บรรทัด)

### ประเภทการแก้ไข
1. ✅ ปิด JSON code block ที่ค้างไว้
2. ✅ เพิ่ม YAML code blocks รอบ test case examples ทั้งหมด
3. ✅ เพิ่ม `##` และ `###` prefix สำหรับ section headers
4. ✅ แปลงข้อความธรรมดาเป็น bulleted lists
5. ✅ แก้ไข blockquote formatting
6. ✅ แก้ไข expected output ให้ตรงกับ engine จริง
7. ✅ ลบบรรทัดว่างเกินออก

### ผลลัพธ์
- ไฟล์ markdown สามารถ parse ได้อย่างถูกต้อง
- Code blocks แสดงผล syntax highlighting ได้
- Section headers มี hierarchy ที่ชัดเจน
- รายการต่างๆ มีรูปแบบที่สอดคล้องกัน
- Content ไม่สูญหาย รักษาความหมายเดิมไว้ทั้งหมด
